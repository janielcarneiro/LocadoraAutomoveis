{{> navbar}}
{{> icons}}

<div class="page">
    <div class="container">
        <div class="mt-5 mb-4">
            <h1>Usuários</h2>
            <hr>
        </div>

        <div class="card">
            <div id="card" class="card-header">
                <svg class="m-1" width="20" height="20"><use xlink:href="#user"/></svg>
                Usuários Cadastrados
            </div>
            <div class="card-body">    
                <table class="table">
                    <thead>
                        <tr>
                        <th scope="col">Nome</th>
                        <th scope="col">CPF</th>
                        <th scope="col">Login</th>
                        <th scope="col">Editar/Excluir</th>
                        </tr>
                    </thead>
                    <tbody>
                    </tbody>
                        {{> tabela_usuarios}}
                </table>
            </div>
        </div>
        <a href="/users/add" class="btn btn-info btn-block mt-5" > Cadastrar novo usuário</a>
    </div>
    
</div>
